{
  "name": "@meridian/pipeline",
  "version": "0.1.0",
  "description": "Survey, diff, compose, revise, publish. Manual trigger only.",
  "type": "module",
  "bin": { "survey": "./dist/run.js" },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "survey": "node dist/run.js"
  },
  "dependencies": { "@meridian/engine": "file:../engine" },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "@types/node": "^22.15.0"
  }
}
